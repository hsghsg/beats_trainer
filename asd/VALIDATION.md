# ASD 验证记录

验证日期：2026-09-23。

- 新增推理代码 Ruff 检查通过，函数均包含功能注释。
- 35 项测试通过，包括阈值边界、非法配置、音频重采样与尾段、两种 PCM 格式、
  任意字节分包、真实 WebSocket 服务、接收超时、异常断开、窗口数限制、
  log-mel/CWT 四后端全链路、线性/MLP 原生头以及独立目录部署。
- 复制的 BEATs、三个分类器及 CWT 核心源文件与项目原文件 SHA-256 一致。
- 使用实际 epoch02 权重及已有三个分类器执行默认 CLI，产生四个后端结果及 JSONL。
- 对项目正常、异常各一个十秒 16 kHz 单声道样本进行真实推理，结果分别为正常、异常。
  对应 embedding 与训练脚本已有缓存的最大绝对差分别为 `7.45e-8`、`3.73e-8`。
  此检查只证明数值兼容性及运行正确性，不作为泛化精度评估。
- 小波使用匹配的微型测试模型验证执行链路；附带实际训练产物使用 log-mel，
  尚未提供小波专用真实训练权重或评估精度。
- Ubuntu 22.04（WSL）/ Python 3.10.12 的 `infer.py --check-config` 通过。
  该环境缺少 PyTorch 等依赖，未进行 Linux 完整推理；openEuler 尚未实机测试。

完整测试环境：Windows、Python 3.12、torch/torchaudio 2.11.0+cu128、
numpy 1.26.4、scikit-learn 1.8.0、websockets 15.0.1。
原项目 BEATs 的 `weight_norm` 使用方式触发第三方弃用提示，不影响上述测试。

本地模型文件共约 663 MB（十进制），已排除在 Git 跟踪之外；部署时须连同
`models/` 一起复制。原训练 checkpoint、分类器及已有未提交改动均未修改。
